# Machine-checking the sampler's math in Lean

`demo/` — Self-contained demo (Colab-ready notebook or markdown). Run without setup.  
`src/` — Full source code, data, and outputs from the experiment execution.

**Type:** proof  
**ID:** `art_-jY8KU1UrYoj`

## Layman Summary

A computer-checked Lean 4 proof of the six mathematical facts behind an anytime reservoir sampler, showing which random choices the algorithm is forced to make and how much randomness it must spend.

## Full Summary

Lean 4 (v4.14.0) + Mathlib formalisation of the load-bearing math behind the anytime reservoir-sampling claims. RESULT: 40/40 declarations compile sorry-free (38 named + 2 sanity `example`s); every named declaration passes `#print axioms` with only the three standard Lean axioms (propext, Classical.choice, Quot.sound) or a strict subset. No `sorryAx`; NO `axiom` is declared anywhere -- every external assumption is an explicit theorem hypothesis. Whole-file elaboration 0.58-0.59 s warm (288 s cold, dominated by `import Mathlib`). Spend $0.

PROVED. (§0) `index_convention`: the prefix-size index i-k+1 and the arriving-item index i+1 differ by exactly k, so `k | (i-k+1)` and `k | (i+1)` name the SAME steps -- retires defect D5, in which fibermap.py and candidates.py used different conventions. (§1) `L0a` k*C(i,k)=(i-k+1)*C(i,k-1) (Johnson biregularity) and `L0b` (i+1)*C(i,k)=(i+1-k)*C(i+1,k), plus cast-discharged real corollaries. (§2) `accept_forced`: the accept coin is FORCED to k/(i+1) in every state (the system is diagonal) -- replaces the 11-cell numerical rank check; `acc_floor` gives the Knuth-Yao floor with Knuth-Yao as an explicit hypothesis. (§3) `theoremC`: ANY column-feasible routing plan preserves prefix uniformity for ALL (n,k) -- converts 'no counterexample below n=18, k<=5' into a theorem; plus `theoremC_existence`, a CONSTRUCTIVE non-emptiness witness f=1/k that re-derives classical Algorithm R's correctness (needed a from-scratch parent count `card_parents`). (§4) `forest_split_count`, stated truncation-free as card+1<=B. (§5) `entropy_le_log_card` proved from scratch via log x <= x-1 (route reported: NOT measure-theoretic, NOT Jensen), giving `per_step_entropy_min`: H_evict <= min(log2 k, log2(k)*k/(i-k+1)) per acceptance, strict for k>=2. (§6) `total_constant_in_n`: total <= log2(k)*k^2 for EVERY n. (§8) `total_sharp`: the SHARP total <= log2(k)*k*H_k = Theta(k log^2 k) by partial fractions, with `harm_le_self` showing it implies §6.

CORRECTIONS REPORTED, NOT BURIED. (1) The hypothesis text's O(k log k) total is WRONG -- the machine-checked sharp bound is Theta(k log^2 k); only the load-bearing CONSTANT-IN-n claim survives. (2) The L4 bound and the exactly solved k=2 minima AGREE: bound 2/i vs minima 1/(i-1), never violated, slack -> 2x (both machine-checked as `example`s). (3) Defect D1's epitaph: the proved bound cannot exceed the trivial ceiling because the min with log2 k is part of the theorem, unlike the fitted law's 11.27 bits at k=1000 against a ceiling of 9.9658. (4) Lean's linter shows `per_step_entropy_bound` never uses hsrc/hHnn and `theoremC` never uses the ROW condition -- uniformity of the next prefix is a COLUMN-SUM condition only.

ASSUMED, NOT PROVED (all as hypotheses, never axioms): Knuth-Yao optimality; concavity => vertex optimum and 'a transportation-polytope vertex has forest support' (both only via the numeric hypothesis E+1<=A+B); Theorem B's modelling hypotheses. Plan feasibility and the max-entropy bound were contingencies and were both DISCHARGED.

DELIVERABLES: proof.lean (= Anytime.lean, 826 lines), proof_out.json (exp_proof_out validated), results/lemma_status.{md,json} (40-row table with #print axioms output and compile times), results/informal_proofs.md (paper-ready LaTeX proofs for every lemma), results/assumed_not_proved.md, results/open_problem.md (poly(k)-time O(k)-space eviction rule attaining the per-step minimum; gap named in both directions), results/index_convention_note.md, results/compiler_warnings.md, results/self_summary.md, verify_small_cases.py (~42k exact-rational pre-checks, 0 failures, incl. Theorem C against RANDOM column-feasible plans), compile_log/ (every compiler invocation).

## Dependencies

- `art_HJNeZFmVR6U1` — background

## Output Files

- `proof.lean`
- `proof_out.json`

## Demo Files

- **proof.lean** — Lean 4 formal proof

---
*Generated by AI Inventor Pipeline*
