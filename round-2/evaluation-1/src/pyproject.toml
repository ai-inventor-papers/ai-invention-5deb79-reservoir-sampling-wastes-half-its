[project]
name = "gen-art-evaluation-1"
version = "0.1.0"
description = "Third detector (L1 identity tester), claims ledger, and validity checks over the two iteration-1 reservoir-sampling experiments"
requires-python = ">=3.12"
dependencies = [
    "loguru==0.7.3",
    "mpmath==1.4.1",
    "numpy==2.5.3",
    "scipy==1.18.1",
]
